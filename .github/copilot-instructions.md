# Pull request description requirements

- Every pull request description must start with `Fix GH-${ISSUE_NUMBER}` on the first line; replace `${ISSUE_NUMBER}` with the linked issue number (for example, `Fix GH-123`).
- Do not include a generic or meaningless `Changes` section in pull request descriptions.
